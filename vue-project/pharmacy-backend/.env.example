PORT=5000
# Use "localhost" when running with `npm start` on the host,
# and "postgres" when running inside docker compose.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=123123
DB_NAME=pharmacy_db
CATEGORY_SERVICE_URL=http://localhost:5001

# Optional alternative to DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME:
# DATABASE_URL=postgres://postgres:123123@localhost:5432/pharmacy_db
